import { StaticPage } from "@/components/static/StaticPage";

// Generic placeholder narrative — swap for the client's real founding story.
export default function StoryPage() {
  return (
    <StaticPage title="Our Story">
      <p>
        SILVERO.925 was started with a simple idea: silver jewellery that&apos;s good enough to wear
        every single day, not just pull out for special occasions.
      </p>
      <p>
        We work exclusively in 925 sterling silver — hallmarked, durable, and finished by hand — so
        every piece holds up to daily wear while still feeling considered.
      </p>
    </StaticPage>
  );
}
